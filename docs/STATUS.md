# Status

## 2026-08-23 현재 상태

- `PROJECT.md`에 Project MicroVillage의 승인된 시스템 명세와 MVP 수락 기준을 기록했다.
- `docs/planning/ROADMAP.md`를 명세 기준의 실행 로드맵으로 재구성했다.
- MVP 목표는 20×20 타일 맵 위에 집·작업장을 배치하고, 주민 한 명이 장애물을 피해 두 시설을 왕복하는 브라우저 프로토타입이다.
- Vite 기반 웹 앱, Canvas 게임 화면, 20×20 그리드 초기 렌더링, 렌더링과 분리된 `GridMap`·`GameState`·`CanvasRenderer` 모듈을 만들었다.
- `npm run test`와 `npm run build`가 통과한다.

## 현재 단계

마일스톤 1: 타일 맵과 입력 시작 전

## 다음 작업

1. 화면·월드·그리드 좌표 변환 함수를 만들고 단위 테스트한다.
2. 타일 호버와 클릭 선택 상태를 Canvas에 표시한다.
3. 기본 카메라 이동과 줌을 추가하기 전에 타일 선택이 정확한지 확인한다.

## 주의 사항

- MVP 완료 기준을 충족하기 전에는 자원 경제, 다수 주민, 저장/불러오기, 고급 UI를 추가하지 않는다.
- 다음 자동 작업 바퀴를 실행하려면 위 목록 중 한 가지 구체적 작업을 `docs/feedback/INBOX.md`에 넣어야 한다.
